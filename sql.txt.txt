
CREATE TABLE CLIENTE( 
	ID INT NOT NULL PRIMARY KEY IDENTITY(1,1), 
	CLI_NOME VARCHAR(70) NOT NULL
	); 

----------

CREATE TABLE VENDA( 
	ID INT NOT NULL PRIMARY KEY IDENTITY(1,1), 
	ID_CLIENTE INT  NOT NULL 	 
	); 

ALTER TABLE VENDA(
	ADD CONSTRAINT FK_CLIENTE FOREING KEY(ID_CLIENTE)
	REFERENCES CLIENTE(ID);
-------------------
CREATE TABLE PRODUTO( 
	ID INT NOT NULL PRIMARY KEY IDENTITY(1,1), 
	PRO_NOME VARCHAR(100) NOT NULL 	 
	); 
---------------
CREATE TABLE ITEM_VENDA( 
	ID INT NOT NULL PRIMARY KEY IDENTITY(1,1), 
	ID_VENDA INT  NOT NULL,
	ID_PRODUTO INT  NOT NULL 	 
	);
ALTER TABLE ITEM_VENDA(
	ADD CONSTRAINT FK_PRODUTO FOREING KEY(ID_PRODUTO)
	REFERENCES PRODUTO(ID);
	
	ADD CONSTRAINT FK_VENDA FOREING KEY(ID_VENDA)
	REFERENCES VENDA(ID);
-------
CREATE TABLE ESTOQUE( 
	ID INT NOT NULL PRIMARY KEY IDENTITY(1,1), 
	ID_PRODUTO INT  NOT NULL,
	QUANTIDADE INT  NOT NULL 	 
	);

ALTER TABLE ESTOQUE(
	ADD CONSTRAINT FK_ESTOQUE FOREING KEY(ID_PRODUTO)
	REFERENCES PRODUTO(ID);
